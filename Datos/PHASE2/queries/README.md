# Queries de investigación de Fase 2

Vacío por ahora. Para investigación de **datos** (matching, coberturas,
formatos de columnas SAP) usa `Datos/PHASE1/queries/` — así se archivó el
descubrimiento del bug de folio en BKPF/EKBE (§22-23), aunque surgió
durante el diseño de Fase 2, porque es un hallazgo de datos, no de
arquitectura.

Esta carpeta es para investigación específica de **Fase 2** que no encaje
en Fase 1 — p. ej. verificar el esquema real de `dm_vendors` antes de correr
`ConsultasBigQuery/HCARB_stg_vendors.sql`, o probar el mecanismo del
webservice del SAT para `HCARB_ESTATUS_SAT` (D24).
